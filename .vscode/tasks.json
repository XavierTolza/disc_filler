{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "Build",
      "type": "shell",
      "command": "cmake . -B build -DCMAKE_BUILD_TYPE=Debug && cd build && make -j6 && cd ..",
      "group": {
        "kind": "build",
        "isDefault": true
      }
    }
  ]
}